__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/
*.db
config/settings.yaml
.env
.pytest_cache/

# Personal committee outputs — archived console responses live here,
# local to your machine (they can contain your own decisions/positions).
research/committee/

# Daily study task log (local runtime output)
study-cycle.log
